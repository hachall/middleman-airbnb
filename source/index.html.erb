---
title: Le Wagon - Middleman
---

<div id="container">
  <div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('https://kitt.lewagon.com/placeholder/cities/london');">
      <div class="banner-content">
        <h1>Welcome home</h1>
        <p>Book unique homes and experience a city like a local.</p>
        <div class="container">
          <form class="form-inline" role="form">
            <input type="text" class="form-control" id="destination" placeholder="Where?">
            <input type="text" class="form-control" id="date" placeholder="When?">
            <select id="favorite" name="language" class="form-control">
                <option value="ruby">1 Guest</option>
                <option value="css">2 Guest</option>
                <option value="javascript">3 Guest</option>
            </select>
            <button type="submit" class="btn btn-primary">Register</button>
          </form>
        </div>
      </div>
    </div>
  <div class="container">
    <div class="row">
    <% data.users.each do |user, info| %>
      <div class="col-xs-12 col-sm-4">
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url(https://kitt.lewagon.com/placeholder/cities/<%= info.city %>);">
            <div class="card-category"><%= info.city %></div>
            <div class="card-description">
              <h2><%= info.name %>'s Home</h2>
              <p><%= info.description %></p>
              <% non_highlighted = 5 - info.stars %>
              <p>
              <% info.stars.times do%>
                <i class="fa fa-star highlighted"></i>
              <% end %>
              <% non_highlighted.times do%>
                <i class="fa fa-star"></i>
              <% end %>
              </p>
            </div>
            <img class="card-user" src="https://kitt.lewagon.com/placeholder/users/<%= info.username %>">
            <a class="card-link" href="#" ></a>
          </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
